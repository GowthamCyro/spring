server:
  port: 9296
  
spring:
  application:
    name: Config-Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GowthamCyro/spring-app-config
          clone-on-start: true